# Simulation results

None. No ngspice run has been performed; the Python simulator and host tests
do not model analog behavior.

Blocked on measured motor resistance, inductance, stall/inrush current, and
travel time; validated TVS and buck models; relay coil data; and fuse
coordination. Add these inputs before running the blocked deck. Save raw
output, plots, and a dated report under `hardware/exports/`.
