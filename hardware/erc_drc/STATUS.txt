ERC/DRC STATUS — real kicad-cli runs (NOT estimates)

Tool: kicad-cli 10.0.6 (macOS arm64, /usr/local/bin/kicad-cli).
Sources: hardware/kicad/na-wink.kicad_sch + na-wink.kicad_pcb (rev 0.3.2;
re-ran 2026-09-24 after buck LMR33630→LM46002-Q1 symbol/value text change +
title-block bump — identical signature to the runs below),
generated by hardware/kicad/generate_kicad.py + audit_design.py.
Reports: hardware/exports/erc.rpt + drc.rpt (this directory's prior
"NOT RUN" note is superseded; rerun commands below reproduce them).

  kicad-cli sch erc --output hardware/exports/erc.rpt hardware/kicad/na-wink.kicad_sch
  kicad-cli pcb drc --output hardware/exports/drc.rpt hardware/kicad/na-wink.kicad_pcb

ERC (2026-09-23): 0 errors, 373 warnings.
  Errors fixed this session: 12x pin_not_connected (relay pole-2, buck SW,
  PROG header) + 2x pin_not_driven (AUX inputs) + 1x pin_to_pin /
  multiple_net_names (a REAL 5V-GND short: the buck VOUT return ran through
  the LDO GND stub label; moved y33->y36, proven by audit_design.py sch
  mode: 1 conflict -> 0).
  Remaining warnings are documented classes, not suppressions:
  endpoint_off_grid (mm-placed review draft), unconnected_wire_endpoint
  (intentional dead-end stubs), lib_symbol/footprint_link (project `na-wink:*`
  names + stock footprints need remap on first KiCad open -- environmental),
  isolated_pin_label (TX0/RX0/BOOT/EN/AUX_GND single-pin intent nets).
  No ERC exclusion was added; no-connect flags exist ONLY on relay pole-2
  (COM2, DPDT-used-as-SPST by documented design).

DRC (2026-09-23): 0 shorting / 0 clearance / 0 crossing errors.
  50x lib_footprint_issues (warnings, environmental: placeholder footprint
  names need stock-library remap, same as ERC).
  22x via_dangling (warnings) + 22x unconnected_items (errors): EVERY one
  sits exactly on a GND pad/via (coordinate-verified against the netlist).
  Cause is PROVEN tool behavior, not a design gap: kicad-cli DRC does not
  fill zones (minimal 2-via-in-zone probe still reports unconnected), so
  the B.Cu GND pour cannot stitch in CLI runs. The pour fills at fab/GUI
  time. All signal/power nets are fully connected (audit_design.py pcb
  mode: 0 diff-net overlaps <0.20mm across 338 copper items).
  8x silk_over_copper (warnings, placeholder refdes offsets) + 0 edge
  violations (2 real ones fixed: w2.0 end caps near y70 + zone outline).
  Generator bug fixed en route: pads emitted bare `(net N)` which KiCad 10
  refuses (`Expected net name`); pads now emit `(net N "name")`.

What CLI DRC does NOT prove (still required): zone fill integrity, real
footprint geometry/courtyard/soldermask (placeholders), creepage on
mains-class gaps, thermal/current capacity of narrowed jogs (0.5-1.0mm
spots are flagged in-generator for HV3 re-check), fab DFM.
Next EDA step (maintainer GUI): open in KiCad >=10, remap libraries, fill
zones, re-run ERC/DRC, export Gerbers. Then bench HV1-HV4.

Independent cross-check (2026-09-23): `kicad-cli sch export netlist`
succeeds (49 components, 41 nets, refs unique; saved as
hardware/exports/netlist-kicad.scm). KiCad's own netlister agrees with
audit_design.py: unlabeled SW wire appears as Net-(L_BUCK-A) (= N_SW),
pole-2 pins as unconnected-(Kx-COM2-Pad6) (by design), all safety nets
pin-correct (e.g. K1.2 coil / K1.3 COM->J2.1 / K1.4 NC->J1.3). A schematic
annotation warning is emitted but the netlist is complete and consistent.
Gerber + drill export (`kicad-cli pcb export gerbers/drill`) also succeeds
(full layer set + .drl + job file; outputs kept in scratch, not committed).

ngspice: SKIPPED — no suitable motor/TVS models; no results invented.
  Behavioral sim only (simulator/). See hardware/sim/ for TVS/buck hand-analysis (no SPICE claim).

ADDENDUM rev 0.3.3 (2026-09-24, attack session): generator changed (C_BULK
value text 35V→50V D-01, R1-R4 560R D-07, D5 revprot added, title rev 0.3.3),
regenerated + re-ran with kicad-cli 10.0.6:
  ERC: 0 errors (380 violations — documented classes only, +7 from the D5
  instance's environmental footprint_link/off-grid warnings; no exclusion added).
  DRC: 0 shorting / 0 clearance / 0 crossing / 0 edge. 23x unconnected_items
  (22 prior + D5.1 GND pad — every endpoint verified GND: same zone-fill
  artifact, D5.1 joins the pour at fill time like all other GND pads) +
  22x via_dangling + 51x lib_footprint (placeholders + D5) + 8x silk.
  audit_design.py: sch 0 multi-label conflicts, pcb 0 diff-net overlaps.
  Netlist export: 50 refs (D5 present; D5.2→/BTN_SENSE, D5.1→/GND —
  antiparallel to opto LED as designed), 41 nets. Mid-route DRC caught one
  REAL self-inflicted short (D5 pad2 exit crossing D5 pad1) — fixed by routing
  north to the BTN_SENSE trunk; final DRC confirms the fix (regression value
  of re-running EDA after every change).
Manual review checklist (first KiCad open):
  [ ] symbol<->footprint remap (MX120G, G2R-2, S3-WROOM-1, SMB, SOT-23, DIP-6)
  [ ] pin numbering vs shell keying (J1/J2/J3)
  [ ] NC/NO orientation per relay (K1-K4) vs PINOUT.md
  [ ] coil polarity + flyback direction (D1-D4)
  [ ] TVS placement at entry, fuse accessibility
  [ ] star GND integrity, motor-track width vs measured stall (HV3)
  [ ] test-point access with case closed
